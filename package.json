{
    "private": true,
    "name": "test-flowtype-vs-typescript",
    "description": "Test project, which shows major differences between: FlowType and TypeScript",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
        "clear": "rm -rf node_modules/ src/flowtype/node_modules/ src/typescript/node_modules/ src/flowtype/bundle.js src/typescript/bundle.js",
        "test": "jasmine test/specs/**",
        "build": "./tasks/build.sh"
    },
    "devDependencies": {
        "jasmine": "^3.4.0"
    }
}
