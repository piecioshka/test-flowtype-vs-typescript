{
    "private": true,
    "scripts": {
        "run": "node bundle.js",
        "build": "webpack",
        "setup": "npm run build && npm run run"
    },
    "devDependencies": {
        "file-loader": "^0.9.0",
        "ts-loader": "^0.8.2",
        "typescript": "^2.0.3",
        "webpack": "^2.0.0-beta"
    }
}
